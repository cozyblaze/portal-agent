// User-owned Portal agent run lifecycle.
// Demos are written to agent_runs/<timestamp>/ and continue across map
// transitions, save loads, deaths, and retries until stop_run is called.

alias start_run "spt_agent_start_run"
alias stop_run "spt_agent_stop_run"

echo "Agent run commands loaded: start_run / stop_run"
